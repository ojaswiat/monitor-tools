__pycache__/
*.pyc
.DS_Store
example/
examples/
temp/
.superpowers/
todo.md

# This repo dogfoods monitor on itself (installed via install-monitor.sh .) —
# the generated data folder and the installed engine copy are project-local,
# not portable source; plugins/monitor/ (the actual source) is unaffected
# since these paths are root-anchored.
/monitor/
/.claude/skills/monitor/
/.claude/commands/monitor/
